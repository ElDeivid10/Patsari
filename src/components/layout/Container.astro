---
interface Props {
  class?: string;
  as?: "div" | "section" | "header" | "footer" | "article" | "main" | "nav";
}

const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class:list={["mx-auto w-full max-w-[1800px] px-4 md:px-6 lg:px-8", className]}>
  <slot />
</Tag>
